import 'package:flutter/material.dart';

class PuzzleGameTable extends StatelessWidget {
  const PuzzleGameTable({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
